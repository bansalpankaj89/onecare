(function() {
    angular.module('mediDocApp.service')
    .factory('serviceName', serviceName);
    serviceName.$inject = ['$q', '$firebaseArray']
    function serviceName($q) {    
        return {
        
        };
    }
})();